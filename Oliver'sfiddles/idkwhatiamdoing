import tkinter as tk
tk.Tk()
window=tk.Tk()
window.geometry("200,300,400")

Example=[20,40]

def addition(numbers):
    
    for number in numbers:
        store_cal = number + number
        
        #so it takes each number in numbers puts them into a calcualtion store which then doses the calcuation then returns it
        



def multiply(numbers):
    return


def divide(numbers):
    return


def subtraction(numbers):
    return    